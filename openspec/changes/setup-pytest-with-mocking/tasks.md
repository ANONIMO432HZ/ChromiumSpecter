# Tasks: setup-pytest-with-mocking

- [x] 1. Install `pytest` and `pytest-mock`.
- [x] 2. Create `tests/conftest.py` with safe defaults for `Path` and `sqlite3`.
- [x] 3. Implement `tests/test_decryptor.py` with mock data for `Local State` and `Login Data`.
- [x] 4. Implement `tests/test_exfiltration.py` mocking `requests.post`.
- [x] 5. Run tests and verify zero disk/network side effects.
